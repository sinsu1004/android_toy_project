<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <!-- 진동 퍼미션-->
    <uses-permission android:name = "android.permission.VIBRATE"/>

    <!-- 인터넷 연결 허용하는 퍼미션 -->
    <uses-permission android:name="android.permission.INTERNET" />



    <application

        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".settings"

        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity
            android:name=".login.loginhome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.Mainscreen"/>
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".activity.bodyspeak_game"/>]
        <activity android:name=".activity.true_count_view"/>
        <activity android:name=".activity.startscreen"/>
        <activity android:name=".activity.startproblem"/>
        <activity android:name=".activity.koreagame"/>
        <activity android:name=".activity.humanquiz"/>
        <activity android:name=".activity.savelist" android:theme="@android:style/Theme.Dialog"/>
    </application>

</manifest>